{
  "name": "tpbx-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:agent": "vite --config vite.agent.config.ts",
    "build": "tsc -b && vite build",
    "build:agent": "vite build --config vite.agent.config.ts && node -e \"require('fs').renameSync('dist-agent/agent.html','dist-agent/index.html')\"",
    "build:ext": "vite build --config vite.ext.config.ts && node -e \"const fs=require('fs');fs.copyFileSync('manifest.chrome.json','dist-ext/manifest.json');fs.copyFileSync('manifest.firefox.json','dist-ext/manifest.firefox.json');\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sip.js": "^0.21.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
