; logger.conf -- managed by TPBX.
;
; Ensures /var/log/asterisk/full exists (used by scripts/diagnose.sh and for
; troubleshooting). Debian's default does not always write a 'full' logfile.

[general]

[logfiles]
console => notice,warning,error,verbose
messages => notice,warning,error
full => notice,warning,error,verbose,debug
