; cel_pgsql.conf -- write Channel Event Log rows directly into PostgreSQL.
;
; Native libpq driver (no ODBC). Requires cel.conf [general] enable=yes.
; password is injected by install.sh.
[general]
hostname=127.0.0.1
port=5432
dbname=tpbx
user=tpbx
password=__DB_PASSWORD__
table=cel
